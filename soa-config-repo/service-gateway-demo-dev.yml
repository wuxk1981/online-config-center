server:
  port: 8081

zuul:
  routes:
    add-service-demo:
      path: /add-service/**
      serviceId: add-service-demo
    random-service-sky:
      path: /random-service/**
      serviceId: random-service-sky

spring:
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
